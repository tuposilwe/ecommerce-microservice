apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    # Container-to-container on the compose network, not localhost.
    url: http://prometheus:9090
    isDefault: true
    editable: false
